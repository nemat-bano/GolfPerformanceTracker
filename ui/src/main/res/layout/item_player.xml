<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="player"
            type="com.sample.domain.model.Player" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/playerCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginVertical="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/playerNameTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{player.name}"
                android:textStyle="bold"
                android:textSize="18sp" />

            <TextView
                android:id="@+id/playerClubTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text='@{"Club: " + player.club}' />

            <TextView
                android:id="@+id/playerAverageSpeedTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text='@{"Average speed: " + String.valueOf(player.averageSpeed) + " mph"}' />

            <TextView
                android:id="@+id/playerAverageDistanceTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text='@{"Average distance: " + String.valueOf(player.averageDistance) + " yards"}' />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

</layout>